{% extends 'base.html.twig' %}

{% block title %}Fotw{% endblock %}

{% block body %}
    <h1>Fotw</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fotw.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fotw.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fotw_index') }}">back to list</a>

    <a href="{{ path('fotw_edit', {'id': fotw.id}) }}">edit</a>

    {{ include('fotw/_delete_form.html.twig') }}
{% endblock %}
